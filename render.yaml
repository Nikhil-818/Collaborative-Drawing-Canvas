services:
  # Next.js Frontend (Static Export)
  - type: static
    name: collab-draw-app
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: out
    envVars:
      - key: NEXT_PUBLIC_WS_URL
        value: https://replace-with-your-railway-url
